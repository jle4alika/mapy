# Mapy — клиент

Expo Router (Android / iOS / Web). Карта MapLibre, друзья в реальном времени, чаты у мест.

```bash
npm install
npx expo start --web
```

API по умолчанию: `EXPO_PUBLIC_API_URL=http://127.0.0.1:8001`
